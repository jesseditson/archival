name: Publish Schemas

on:
  push:
    tags:
      - "*"

jobs:
  archival_template:
    name: Publish archival_template.schema.json
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO"
      # TODO: check if this schema has changed
      # If it has, publish a new PR to
      # https://github.com/SchemaStore/schemastore/pulls
  archival:
    name: Publish archival.schema.json
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO"
      # TODO: check if this schema has changed
      # If it has, publish a new PR to
      # https://github.com/SchemaStore/schemastore/pulls
  archival_objects:
    name: Publish archival_objects.schema.json
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO"
      # TODO: check if this schema has changed
      # If it has, publish a new PR to
      # https://github.com/SchemaStore/schemastore/pulls
